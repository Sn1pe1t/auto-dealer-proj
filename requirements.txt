# Веб-фреймворк
flask>=3.0.0

# Тестирование
pytest>=8.0.0
pytest-cov>=5.0.0

# Установка локального пакета autodealer-core (для импорта autodealer_core)
-e .

# Работа с базой данных (обычно встроен в Python, 
# но если используете расширения, можно указать)
# sqlite3 включен в стандартную библиотеку Python